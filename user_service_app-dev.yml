spring.jpa.hibernate.ddl-auto: update
spring.datasource:
  url: jdbc:mysql://localhost:3307/tweet?allowPublicKeyRetrieval=true&useSSL=false
  username: root
  password: root
  driver-class-name: com.mysql.jdbc.Driver
spring.batch.initialize-schema: always

eureka:
  client:
    enabled: false
server.port: 8082
